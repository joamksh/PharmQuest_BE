# Copy to application-local-secrets.properties (ignored by Git).
# Use the decoded data.go.kr service key.
# OpenAI: set this property or the OPENAI_API_KEY environment variable.
# openai.api-key=
openapi.medicine.api-key=
fda.api.api-key=
google.cloud.translate.api-key=
# Enable only after setting the actual translation API key.
google.cloud.translate.enabled=false
